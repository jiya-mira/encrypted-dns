# tools/pyproject.toml
[project]
name = "encrypted-dns-tools"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
  "cryptography>=42,<48",
  "questionary>=2,<3",
  "tqdm>=4.66,<5",
]

[project.scripts]
sign-profiles = "sign_profiles:main"